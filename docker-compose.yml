version: '2'
services:
  web:
    build: .
    ports:
    - "3000:3000"
    volumes:
    - .:/code
    - logvolume01:/var/log
    links:
    - redis
  mongo:
    image: mongo
volumes:
  logvolume01: {}