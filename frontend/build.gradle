plugins {
    id "com.moowork.node" version "0.13"
}

apply plugin: 'base'
version '0.0.1'

node {
    version = '7.0.0'
    npmVersion = '3.10.9'
    download = true
}

assemble.dependsOn(npm_run_build)
