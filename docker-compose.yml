version: '2'

networks:
  backend:
  frontend:

services:
  backend:
    image: mongo:latest
    build: /backend/build
    ports:
      - "8086:8080"
  frontend:
    build: /backend/build
    ports:
      - "3001:3000"