sudo: required

matrix:
  include:
    - os: linux
      dist: trusty
#    - os: osx
#      env: OSX=10.12
#      osx_image: xcode8.2

#services:
#  - docker

script:
#   Checking os info
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then system_profiler SPSoftwareDataType; fi

  - docker -v
  - docker-compose -v
#  - docker-machine -v
  - docker info
  - docker images -a
  - docker ps -a
  - docker network ls
  - docker volume ls

  - docker-compose up -d

after_script:
  - docker images -a
  - docker ps -a
  - docker volume ls
  - docker network ls
  - docker network inspect courses_net
  - docker info