version: 1.0.{build}
platform:
#  - x86
  - x64
build:
  verbosity: detailed

install:
  - cup all -v
  - cinst -y jdk8 nodejs.install
  - refreshenv

test_script:
  - cd backend; dir; gradlew.bat clean build; dir
  - cd frontend; dir; npm run build

branches:
  only:
    - master

#cache:
#    - node_modules -> frontend\package.json

environment:
  JAVA_HOME: C:\Program Files\Java\jdk1.8.0
